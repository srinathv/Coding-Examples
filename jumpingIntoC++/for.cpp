#include <iostream>

int main () {
    // The loop goes while i < 10, increasing i by one each loop
  for ( int i = 0 ; i < 10 ; i++ ) {
    // The condition is checked each loop
    // When i equals 10, then the loop breaks.
    std::cout << i << " squared is " << i * i << std::endl ;
  }
}
     
